.window {
  background-color white
  border-radius 10px
  width 600px
  height 400px
  margin-top: 80px
  padding 40px
  box-shadow: 8px 8px 4px 0 rgba(0, 0, 0, 0.3);

  display grid
  grid-column 1
  justify-items: center;
}